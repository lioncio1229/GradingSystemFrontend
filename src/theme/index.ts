import { createTheme } from '@mui/material/styles';


const theme = createTheme({
    palette: {
        primary: {
            light: "#b75575",
            main: "#8A4A64",
            dark: "#614151"
        },
        secondary: {
            light: "#d68167",
            main: "#dda493",
            dark: "#eac7bd"
        }
    }
});

export default theme;
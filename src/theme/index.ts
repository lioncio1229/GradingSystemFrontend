import { createTheme } from '@mui/material/styles';


const theme = createTheme({
    palette: {
        primary: {
            light: "#b75575",
            main: "#8A4A64",
            dark: "#614151"
        },
        secondary: {
            light: "#eac7bd",
            main: "#dda493",
            dark: "#d68167",
        }
    }
});

export default theme;